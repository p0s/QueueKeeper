{
  "name": "@queuekeeper/shared",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "pnpm exec biome lint src package.json"
  },
  "devDependencies": {
    "typescript": "5.8.2"
  }
}
