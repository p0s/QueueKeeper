# Cover asset

Primary repo-side cover asset:

- `queuekeeper-cover.svg`

If the submission form requires a hosted image URL, use the live Open Graph asset:

- `https://queuekeeper.xyz/opengraph-image`

If the submission form insists on PNG upload, export `queuekeeper-cover.svg` to PNG before upload.
