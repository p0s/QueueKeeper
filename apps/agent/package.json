{
  "name": "@queuekeeper/agent",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@selfxyz/core": "1.2.0-beta.1",
    "@queuekeeper/core": "workspace:*",
    "@queuekeeper/shared": "workspace:*",
    "dotenv": "16.4.7",
    "viem": "2.23.5"
  },
  "devDependencies": {
    "@types/node": "22.13.10",
    "tsx": "4.19.3",
    "typescript": "5.8.2"
  }
}
